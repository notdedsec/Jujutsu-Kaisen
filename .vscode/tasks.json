{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "vspreview",
            "type": "shell",
            "command": "${command:python.interpreterPath}",
            "args": ["-m", "vspreview", "\"'${file}'\""]
        },
        {
            "label": "update_common",
            "type": "shell",
            "command": "${command:python.interpreterPath}",
            "args": ["-m", "pip", "install", "--upgrade", "-e", "."]
        }
    ]
}
